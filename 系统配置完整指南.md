# 鲁班 Skills 系统配置完整指南

> 📚 **适合人群**：零基础小白、想了解 AI 任务执行系统的开发者
> 📅 **版本**：v1.3.0
> 🎯 **目标**：让你完全理解这个系统是什么、怎么配置、怎么使用

---

## 📖 目录

1. [系统简介](#1-系统简介)
2. [核心概念](#2-核心概念)
3. [系统架构](#3-系统架构)
4. [环境配置](#4-环境配置)
5. [目录结构详解](#5-目录结构详解)
6. [配置文件说明](#6-配置文件说明)
7. [快速上手](#7-快速上手)
8. [进阶使用](#8-进阶使用)
9. [常见问题](#9-常见问题)
10. [最佳实践](#10-最佳实践)

---

## 1. 系统简介

### 1.1 这是什么？

**鲁班 Skills** 是一个 AI 任务执行教练系统，它能把你的一句模糊需求，变成可执行、可追踪、可验收的完整流程。

**类比理解**：
- 就像你请了一个项目经理，你只需要说"我要写一封邮件"
- 系统会自动帮你：
  1. 理解你的需求
  2. 拆解成具体步骤
  3. 调度 AI 执行
  4. 产出最终文件
  5. 验收结果

### 1.2 能做什么？

| 场景类型     | 具体例子               | 最终得到什么           |
| ------------ | ---------------------- | ---------------------- |
| **写作类**   | 写邮件、写报告、写方案 | 可以直接复制使用的文档 |
| **整理类**   | 整理会议记录、提炼资料 | 结构化的清单和要点     |
| **分析类**   | 分析表格数据、对比方案 | 结论 + 依据 + 建议     |
| **规划类**   | 制定项目计划、拆解任务 | 里程碑 + 任务清单      |
| **多图成品** | 生成连贯漫画、系列海报 | 实际的图片文件         |

### 1.3 不能做什么？

以下情况**不适合**使用本系统：
- ❌ 删除、覆盖等高风险操作
- ❌ 生产环境的直接变更
- ❌ 医疗、法律、金融等专业判断
- ❌ 没有明确目标的闲聊

---

## 2. 核心概念

### 2.1 三个关键角色

```
用户 → OpenClaw → ACP → Hermes → 交付成果
      (理解需求)  (传递)  (执行任务)
```

| 角色         | 职责                         | 类比         |
| ------------ | ---------------------------- | ------------ |
| **OpenClaw** | 接收需求、拆解任务、维护沟通 | 项目经理     |
| **ACP**      | 任务传递、状态管理、协同控制 | 任务调度系统 |
| **Hermes**   | 具体执行、文件操作、工具调用 | 执行工程师   |

### 2.2 六步工作流

```
第1步：识别任务类型（写作/整理/分析/规划/执行辅助）
  ↓
第2步：明确最终交付物（要产出什么文件/内容）
  ↓
第3步：抽取约束条件（可用目录、格式要求、禁止项）
  ↓
第4步：先做最小闭环（最小可验证版本）
  ↓
第5步：分阶段执行（理解→执行→检查→交付）
  ↓
第6步：验收并交付（确认符合标准）
```

### 2.3 任务卡（Task Card）

任务卡是系统的核心配置文件，包含：
- 任务名称和类型
- 最终目标
- 工作目录
- 输入材料
- 约束条件
- 验收标准

---

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────┐
│              用户输入需求                      │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│           OpenClaw (任务理解)                 │
│  - 识别任务类型                               │
│  - 生成任务卡                                 │
│  - 设置约束条件                               │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│         ACP (任务调度与传递)                   │
│  - 任务传递                                   │
│  - 状态管理                                   │
│  - 进度回报                                   │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│          Hermes (任务执行)                    │
│  Phase 1: Intent (理解目标)                  │
│  Phase 2: ReAct (循环执行)                   │
│  Phase 3: Reflect (复盘检查)                 │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│         交付成果 (文件/报告/图片)              │
└─────────────────────────────────────────────┘
```

### 3.2 数据流向

```
任务卡 YAML
  ↓
build_prompt.sh (生成 Hermes Prompt)
  ↓
dispatch_acp.sh (启动调度)
  ↓
acp_pty_driver.py (ACP 客户端)
  ↓
Hermes 执行
  ↓
runs/<session-id>/ (结果归档)
```

---

## 4. 环境配置

### 4.1 系统要求

- **操作系统**：Linux (推荐) / macOS
- **Shell**：bash
- **Python**：3.8+
- **必需工具**：
  - `openclaw` 命令行工具
  - `bash`
  - `python3`

### 4.2 环境变量配置

创建 `.env` 文件（位于 luban-skills 根目录）：

```bash
# 飞书应用配置（用于通知）
FEISHU_APP_ID=your_app_id
FEISHU_APP_SECRET=<feishu-app-secret>

# 飞书 Webhook URL（用于接收任务状态通知）
FEISHU_WEBHOOK_URL=https://example.com/feishu-webhook
```

**说明**：
- 如果不需要飞书通知，可以不配置这些变量
- 系统会自动从多个位置查找 `.env` 文件：
  1. `luban-skills/.env`
  2. `diting-skills/.env`
  3. `~/.openclaw/.env`

### 4.3 目录权限

确保以下目录可写：
```bash
chmod +x scripts/*.sh
chmod +x scripts/*.py
```

### 4.4 自检命令

运行系统自检：
```bash
cd <luban-skills-dir>
bash scripts/doctor.sh
```

预期输出：
```
✓ bash 可用
✓ python3 可用
✓ scripts/ 目录存在
✓ references/ 目录存在
✓ examples/ 目录存在
```

---

## 5. 目录结构详解

```
luban-skills/
├── SKILL.md                    # 系统核心说明文档
├── README.md                   # 对外分享说明
├── QUICKSTART.md               # 快速上手指南
├── CHANGELOG.md                # 版本更新记录
├── _meta.json                  # Skill 元信息
├── .env                        # 环境变量配置（需自己创建）
│
├── examples/                   # 示例任务
│   ├── example-write-email.md          # 写邮件示例
│   ├── example-organize-docs.md        # 整理文档示例
│   ├── example-plan-project.md         # 项目规划示例
│   └── task-card-example.yaml          # 任务卡模板示例
│
├── references/                 # 参考文档和模板
│   ├── task-card-template.md           # 任务卡模板
│   ├── delivery-templates.md           # 交付物模板
│   ├── hermes-three-phase-prompt.md    # Hermes 执行模板
│   ├── user-facing-language.md         # 用户交互语言规范
│   ├── safety-boundary.md              # 安全边界规则
│   ├── acp-long-task-checklist.md      # 长任务检查清单
│   ├── feishu-notification.md          # 飞书通知规范
│   ├── llm-behavior-rules.md           # LLM 行为规范
│   └── allowlist-default.yaml          # 工具白名单
│
├── scripts/                    # 执行脚本
│   ├── doctor.sh                       # 系统自检
│   ├── classify_task.sh                # 任务类型识别
│   ├── build_prompt.sh                 # Prompt 构建器
│   ├── dispatch_acp.sh                 # 主调度器 ⭐
│   ├── acp_pty_driver.py               # ACP 客户端驱动
│   ├── guard.py                        # 工具调用审批
│   ├── notify_feishu.sh                # 飞书通知
│   └── verify_run.sh                   # 结果验收
│
├── runs/                       # 执行记录（自动生成）
│   └── <session-id>/
│       ├── prompt.txt                  # 最终 Prompt
│       ├── stdout.log                  # 完整输出日志
│       ├── events.jsonl                # 事件流
│       ├── state.json                  # 状态快照
│       └── client.pid                  # 进程 PID
│
├── tests/                      # 测试文件
└── tmp/                        # 临时文件

---

## 6. 配置文件说明

### 6.1 任务卡配置 (task-card.yaml)

这是最重要的配置文件，定义了一个任务的所有信息。

**完整示例**：

```yaml
# 任务标识
task_label: demo-write-email

# 任务类型（5选1）
task_type: 写作类  # 写作类/整理类/分析类/规划类/执行辅助类

# 最终目标（一句话说清楚要产出什么）
goal: 根据会议纪要生成一封200字内的项目推进邮件

# 工作目录
workdir: <workspace>

# 可读目录列表
readable_dirs:
  - <workspace>
  - <workspace>/docs

# 可写目录列表（为空表示只读任务）
writable_dirs: []

# 输入材料
input_materials:
  - <workspace>/meeting-notes.md

# 约束条件
constraints:
  - 语气正式
  - 不超过200字
  - 分三段以内
  - 不要使用emoji

# 风险点
risks:
  - 会议纪要可能信息不完整
  - 需要确认收件人范围

# 第一步动作（最小、最安全的第一步）
first_action: 先读取会议纪要，提炼3个关键点

# 验收标准
acceptance:
  - 生成一封可直接复制发送的邮件
  - 包含背景、当前进展、下一步动作
  - 字数在200字以内
  - 语气正式得体

# 时间预算（秒）
time_budget_s: 600

# 是否为哨兵任务（一般设为false）
sentinel: false
```

**字段详解**：

| 字段              | 必填 | 说明         | 示例               |
| ----------------- | ---- | ------------ | ------------------ |
| `task_label`      | ✅    | 任务唯一标识 | `demo-write-email` |
| `task_type`       | ✅    | 任务类型     | `写作类`           |
| `goal`            | ✅    | 最终目标     | `生成一封邮件`     |
| `workdir`         | ✅    | 工作目录     | `<workspace>`      |
| `readable_dirs`   | ✅    | 可读目录     | 列表               |
| `writable_dirs`   | ✅    | 可写目录     | 列表（可为空）     |
| `input_materials` | ❌    | 输入文件     | 列表               |
| `constraints`     | ❌    | 约束条件     | 列表               |
| `risks`           | ❌    | 风险点       | 列表               |
| `first_action`    | ✅    | 第一步动作   | 字符串             |
| `acceptance`      | ✅    | 验收标准     | 列表               |
| `time_budget_s`   | ✅    | 时间预算     | 数字（秒）         |
| `sentinel`        | ✅    | 哨兵模式     | `true/false`       |

### 6.2 环境变量配置 (.env)

```bash
# 飞书应用配置
FEISHU_APP_ID=<feishu-app-id>              # 飞书应用ID
FEISHU_APP_SECRET=<feishu-app-secret>              # 飞书应用密钥
FEISHU_WEBHOOK_URL=https://example.com/feishu-webhook       # Webhook地址

# ACP 配置（可选）
ACP_TIMEOUT_S=1200                   # ACP超时时间（秒）
ACP_MAX_RETRIES=3                    # 最大重试次数

# 调试模式（可选）
DEBUG=false                          # 是否开启调试
DRY_RUN=false                        # 是否只生成prompt不执行
```

### 6.3 工具白名单配置 (allowlist-default.yaml)

控制 Hermes 可以使用哪些工具：

```yaml
# 允许的工具列表
allowed_tools:
  - read_file
  - list_directory
  - search_files
  - write_file  # 谨慎开启

# 禁止的工具列表
blocked_tools:
  - delete_file
  - execute_command
  - modify_system

# 需要审批的工具
approval_required:
  - write_file
  - create_directory
```

---

## 7. 快速上手

### 7.1 第一个任务：写邮件

**步骤1：准备输入材料**

创建一个会议纪要文件：
```bash
cat > /tmp/meeting-notes.md << 'EOF'
# 项目推进会议纪要

时间：2026-04-26
参会人：张三、李四、王五

讨论内容：
1. 项目进度：已完成需求分析，正在进行设计阶段
2. 遇到问题：设计方案需要技术评审
3. 下一步：本周五前完成技术评审，下周一开始开发


---

## 附录 A：当前系统 OpenClaw 配置

### A.1 OpenClaw 基本信息

```bash
# OpenClaw 安装路径
<openclaw-bin>

# 配置文件路径
<openclaw-config>

# 版本信息
最后更新：2026-04-22
版本：2026.4.14
```

### A.2 核心配置

#### 网关配置
```json
{
  "gateway": {
    "auth": {
      "mode": "token",
      "token": "<gateway-token>"
    },
    "mode": "local"
  }
}
```

#### Agent 默认配置
```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "my-gpt5.4/gpt-5.5",
        "fallbacks": []
      },
      "workspace": "<workspace>",
      "memorySearch": {
        "provider": "local",
        "fallback": "none",
        "cache": {
          "enabled": true
        }
      },
      "compaction": {
        "mode": "safeguard"
      },
      "heartbeat": {
        "every": "55m"
      },
      "maxConcurrent": 2,
      "subagents": {
        "maxConcurrent": 3
      },
      "imageGenerationModel": {
        "primary": "openai/gpt-image-2"
      }
    }
  }
}
```

### A.3 已配置的 Agent 列表

| Agent ID             | 名称           | 工作目录                  | 运行时       |
| -------------------- | -------------- | ------------------------- | ------------ |
| `agent_fengxinzi_pm` | 疯信子项目经理 | `<workspace>`             | ACP (Hermes) |
| `hermes`             | Hermes Agent   | `<hermes-workspace>`      | ACP (Hermes) |
| `agent_image_studio` | 图片生成助手   | `<agent-image-workspace>` | 默认         |

### A.4 ACP 配置（重要）

```json
{
  "plugins": {
    "entries": {
      "acpx": {
        "enabled": true,
        "config": {
          "timeoutSeconds": 1800,
          "permissionMode": "approve-all",
          "nonInteractivePermissions": "deny"
        }
      },
      "adp-openclaw": {
        "enabled": true
      }
    }
  }
}
```

**说明**：
- `timeoutSeconds: 1800` - ACP 任务超时时间为 30 分钟
- `permissionMode: "approve-all"` - 所有工具调用需要审批
- `nonInteractivePermissions: "deny"` - 非交互式权限默认拒绝

### A.5 模型配置

#### 主要模型提供商
```json
{
  "models": {
    "providers": {
      "my-gpt5.4": {
        "baseUrl": "https://example.com/v1",
        "apiKey": "<api-key>",
        "api": "openai-completions",
        "models": [
          {
            "id": "gpt-5.5",
            "name": "GPT-5.5"
          },
          {
            "id": "gpt-image-2",
            "name": "GPT Image 2"
          }
        ]
      }
    }
  }
}
```

### A.6 已启用的插件

| 插件名称           | 版本   | 用途             |
| ------------------ | ------ | ---------------- |
| `adp-openclaw`     | 0.0.77 | ACP 协议支持     |
| `acpx`             | -      | ACP 扩展         |
| `feishu`           | -      | 飞书集成         |
| `telegram`         | -      | Telegram 集成    |
| `discord`          | -      | Discord 集成     |
| `openclaw-weixin`  | 2.1.8  | 微信集成         |
| `browser`          | -      | 浏览器控制       |
| `memory-tencentdb` | 0.2.0  | 腾讯云数据库记忆 |

### A.7 浏览器配置

```json
{
  "browser": {
    "enabled": true,
    "executablePath": "<chrome-bin>",
    "noSandbox": true,
    "defaultProfile": "user",
    "profiles": {
      "user": {
        "cdpUrl": "http://localhost:<port>",
        "driver": "existing-session",
        "attachOnly": true,
        "userDataDir": "<browser-profile-dir>"
      }
    }
  }
}
```

---

## 附录 B：当前系统 Hermes 配置

### B.1 Hermes 基本信息

```bash
# Hermes 版本
Hermes Agent v0.10.0 (2026.4.16)

# 安装路径
<hermes-bin>

# 项目目录
<hermes-workspace>

# Python 版本
Python 3.11.6

# OpenAI SDK 版本
OpenAI SDK: 2.32.0
```

### B.2 Hermes 在 OpenClaw 中的配置

```json
{
  "id": "hermes",
  "name": "Hermes Agent",
  "runtime": {
    "type": "acp",
    "acp": {
      "agent": "hermes-agent",
      "backend": "acpx",
      "mode": "persistent",
      "cwd": "<hermes-workspace>"
    }
  },
  "workspace": "<hermes-workspace>",
  "model": {
    "primary": "my-gpt5.4/gpt-5.5",
    "fallbacks": []
  }
}
```

### B.3 Hermes 运行模式

| 参数      | 值                   | 说明           |
| --------- | -------------------- | -------------- |
| `type`    | `acp`                | 使用 ACP 协议  |
| `agent`   | `hermes-agent`       | Agent 类型     |
| `backend` | `acpx`               | 使用 ACPX 后端 |
| `mode`    | `persistent`         | 持久化模式     |
| `cwd`     | `<hermes-workspace>` | 工作目录       |

### B.4 Hermes 更新状态

```bash
# 当前状态
Update available: 736 commits behind

# 更新命令
hermes update
```

---

## 附录 C：环境变量配置

### C.1 OpenClaw 环境变量

位置：`<openclaw-env-file>`

```bash
# Memos API
MEMOS_API_KEY=<memos-api-key>

# Kimi API
export KIMI_API_KEY=<kimi-api-key>

# 腾讯文档 Token
export TENCENT_DOCS_TOKEN="<tencent-docs-token>"
```

### C.2 Luban Skills 环境变量

位置：`<luban-skills-dir>/.env`

```bash
# 飞书应用配置
FEISHU_APP_ID=<feishu-app-id>
FEISHU_APP_SECRET=<feishu-app-secret>

# 飞书 Webhook URL
FEISHU_WEBHOOK_URL=https://example.com/feishu-webhook
```

---

## 附录 D：完整配置检查清单

### D.1 OpenClaw 配置检查

```bash
# 1. 检查 OpenClaw 是否安装
which openclaw

# 2. 检查配置文件
ls -la ~/.openclaw/openclaw.json

# 3. 检查 ACP 插件
grep -n '"acpx"' <openclaw-config>

# 4. 检查 Agent 列表
grep -n '"agents"' <openclaw-config>

# 5. 检查模型配置
grep -n '"models"' <openclaw-config>
```

### D.2 Hermes 配置检查

```bash
# 1. 检查 Hermes 是否安装
which hermes

# 2. 检查版本
hermes --version

# 3. 检查工作目录
ls -la <hermes-workspace>

# 4. 检查 Hermes 在 OpenClaw 中的配置
grep -n '"hermes"' <openclaw-config>
```

### D.3 Luban Skills 配置检查

```bash
# 1. 进入 luban-skills 目录
cd <luban-skills-dir>

# 2. 运行自检
bash scripts/doctor.sh

# 3. 检查环境变量键名是否存在，不打印真实值
grep -E '^(FEISHU_|ACP_|DRY_RUN)' .env | cut -d= -f1

# 4. 检查脚本权限
ls -la scripts/

# 5. 测试任务类型识别
bash scripts/classify_task.sh "测试任务"
```

### D.4 集成测试

```bash
# 1. 测试 OpenClaw → ACP → Hermes 链路
cd <luban-skills-dir>

# 2. 创建测试任务卡
cat > /tmp/test-task.yaml << 'EOF'
task_label: integration-test
task_type: 执行辅助类
goal: 测试 OpenClaw 和 Hermes 集成
workdir: /tmp
readable_dirs:
  - /tmp
writable_dirs: []
input_materials: []
constraints:
  - 只读操作
first_action: 列出当前目录
acceptance:
  - 成功列出目录内容
time_budget_s: 60
sentinel: false
EOF

# 3. 执行测试
bash scripts/dispatch_acp.sh start /tmp/test-task.yaml

# 4. 查看结果
ls -lt runs/ | head -5
```

---

## 附录 E：常用配置修改

### E.1 修改 ACP 超时时间

编辑 `<openclaw-config>`：

```json
{
  "plugins": {
    "entries": {
      "acpx": {
        "config": {
          "timeoutSeconds": 3600  // 改为 1 小时
        }
      }
    }
  }
}
```

### E.2 修改默认模型

编辑 `<openclaw-config>`：

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "my-gpt5.4/gpt-5.5",  // 修改为你想要的模型
        "fallbacks": []
      }
    }
  }
}
```

### E.3 添加新的 Agent

编辑 `<openclaw-config>`：

```json
{
  "agents": {
    "list": [
      {
        "id": "my-new-agent",
        "name": "我的新助手",
        "workspace": "/path/to/workspace",
        "model": {
          "primary": "my-gpt5.4/gpt-5.5",
          "fallbacks": []
        }
      }
    ]
  }
}
```

### E.4 修改 Hermes 工作目录

编辑 `<openclaw-config>`，找到 hermes agent 配置：

```json
{
  "id": "hermes",
  "runtime": {
    "acp": {
      "cwd": "/new/path/to/hermes"  // 修改工作目录
    }
  },
  "workspace": "/new/path/to/hermes"
}
```

### E.5 启用/禁用插件

编辑 `<openclaw-config>`：

```json
{
  "plugins": {
    "entries": {
      "feishu": {
        "enabled": false  // 禁用飞书插件
      },
      "telegram": {
        "enabled": true   // 启用 Telegram 插件
      }
    }
  }
}
```

---

## 附录 F：故障排查配置相关问题

### F.1 ACP 连接失败

**症状**：任务启动后提示 ACP 连接失败

**检查步骤**：
```bash
# 1. 检查 acpx 插件是否启用
grep -n '"acpx"' <openclaw-config>

# 2. 检查 adp-openclaw 插件
grep -n '"adp-openclaw"' <openclaw-config>

# 3. 重启 OpenClaw 服务
# (根据你的部署方式重启)
```

### F.2 Hermes 无法启动

**症状**：调度 Hermes 时失败

**检查步骤**：
```bash
# 1. 检查 Hermes 是否安装
which hermes

# 2. 检查 Hermes 配置
grep -n '"hermes"' <openclaw-config>

# 3. 手动测试 Hermes
hermes --version

# 4. 检查工作目录是否存在
ls -la <hermes-workspace>
```

### F.3 模型调用失败

**症状**：任务执行时提示模型错误

**检查步骤**：
```bash
# 1. 检查模型配置
grep -n '"models"' <openclaw-config>

# 2. 检查 API Key 是否已配置，不要把真实值粘贴到公开文档
# env 文件仅在本机查看，不提交到仓库

# 3. 测试 API 连接
curl -X POST https://example.com/v1/chat/completions \
  -H "Authorization: Bearer <api-key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-5.5","messages":[{"role":"user","content":"test"}]}'
```

### F.4 权限问题

**症状**：任务执行时提示权限不足

**检查步骤**：
```bash
# 1. 检查脚本权限
ls -la <luban-skills-dir>/scripts/

# 2. 修复权限
chmod +x <luban-skills-dir>/scripts/*.sh
chmod +x <luban-skills-dir>/scripts/*.py

# 3. 检查目录权限
ls -la <luban-skills-dir>/
```

---

**配置部分完成！** 🎉

现在你已经了解了完整的系统配置，包括：
- OpenClaw 的详细配置
- Hermes 的集成配置
- 环境变量设置
- 配置检查清单
- 常用配置修改方法
- 故障排查指南
